spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/aichat?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&connectionCollation=utf8mb4_unicode_ci}
    username: ${DB_USER:root}
    password: fuck520c
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  web:
    resources:
      cache:
        use-last-modified: false
  ai:
    dashscope:
      agent:
        app-id: d8cd5f81725542739d3a898dff4d9044
      api-key: sk-08e63f7e59aa496e82897dd6c849d544

app:
  security:
    jwt:
      secret: ${JWT_SECRET:replace-this-with-at-least-32-characters-secret-key}
      expiration-seconds: ${JWT_EXPIRATION_SECONDS:7200}
    users:
      admin:
        username: ${ADMIN_USERNAME:admin}
        password: ${ADMIN_PASSWORD:123456}
      user:
        username: ${USER_USERNAME:user}
        password: ${USER_PASSWORD:123456}
      experts:
        - username: ${EXPERT1_USERNAME:linyu}
          password: ${EXPERT1_PASSWORD:123456}
        - username: ${EXPERT2_USERNAME:chenmo}
          password: ${EXPERT2_PASSWORD:123456}
        - username: ${EXPERT3_USERNAME:jiangning}
          password: ${EXPERT3_PASSWORD:123456}
        - username: ${EXPERT4_USERNAME:suwan}
          password: ${EXPERT4_PASSWORD:123456}
        - username: ${EXPERT5_USERNAME:qiaoan}
          password: ${EXPERT5_PASSWORD:123456}

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  port: ${SERVER_PORT:8090}
